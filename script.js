/* 画像の左列（用語）と右列（解説・意味）を対応付けた問題データ。 */
const problems = [
 [1,'DATE(年,月,日)','指定した日付のシリアル値を求める。'],[2,'YEAR(シリアル値)','指定したシリアル値の年を求める。'],[3,'MONTH(シリアル値)','指定したシリアル値の月を求める。'],[4,'DAY(シリアル値)','指定したシリアル値の日を求める。'],[5,'WEEKDAY(シリアル値)','指定した日付の曜日を求める。'],[6,'TIME(時,分,秒)','指定した時刻のシリアル値を求める。'],[7,'HOUR(シリアル値)','指定したシリアル値の時を求める。'],[8,'MINUTE(シリアル値)','指定したシリアル値の分を求める。'],[9,'SECOND(シリアル値)','指定したシリアル値の秒を求める。'],[10,'INT(数値)','引数によって示された数値の小数点以下を切り捨て、その数値を超えない最大の整数を求める。'],[11,'MOD(数値,除数)','数値を除数で割った余りを求める。'],[12,'SUMIFS(合計対象範囲,条件範囲1,条件1,・・・)','複数の条件に一致する数値の合計を求める。'],[13,'AVERAGEIFS(平均対象範囲,条件範囲1,条件1,・・・)','複数の条件に一致する数値の平均を求める。'],[14,'COUNTIFS(検索条件範囲1,検索条件1,・・・)','複数の条件に一致するセルの個数を求める。'],[15,'LARGE(配列,順位)','配列（範囲）内の順位（n）番目に大きい数値を求める。'],[16,'SMALL(配列,順位)','配列（範囲）内の順位（n）番目に小さい数値を求める。'],[17,'VLOOKUP(検索値,範囲,列番号,検索方法[TRUEまたはFALSE])','範囲内の左端の列を上から下方向に検索し、検索値と一致（完全一致）した場合は、列番号のデータを参照し、一致しない場合は、検索値より小さいデータの中の最大値を一致するデータとみなし（近似一致）で参照する。'],[18,'HLOOKUP(検索値,範囲,列番号,検索方法[TRUEまたはFALSE])','範囲内の上端の行を左から右方向に検索し、検索値と一致（完全一致）した場合は、行番号のデータを参照し、一致しない場合は、検索値より小さいデータの中の最大値を一致するデータとみなし（近似一致）で参照する。'],[19,'INDEX(配列,行番号,列番号)','指定した行番号と列番号が交差したセルのデータを参照する。'],[20,'MATCH(検査値,検索範囲,照合の種類)','検査値範囲を検索し、検査値と一致する値の相対的な位置を表す数値を参照する。'],[21,'TEXT(値,表示形式)','値を指定した表示形式に変換する。'],[22,'FIND(検索文字列,対象,開始位置)','セル内のデータのうち、文字列がそのデータ内の開始位置から何番目にあるかを検索する。（全角・半角、大文字と小文字を区別する。）'],[23,'SEARCH(検索文字列,対象,開始位置)','セル内のデータのうち、文字列がそのデータ内の開始位置から何番目にあるかを検索する。（全角・半角、大文字と小文字を区別しない。）'],[24,'論理演算子(AND, OR, NOT)','AND･･･すべての条件を満たす。 OR･･･いずれかの条件を満たす。 NOT･･･条件を満たさない。'],[25,'CELING(数値,基準値)','基準値の倍数のうち、数値以上の最小値を表示する。ただし、基準値が負、数値が正の場合、エラーとなり、基準値と数値が負の場合、数値以下の最大値となる。'],[26,'FLOOR(数値,基準値)','基準値の倍数のうち、数値以下の最大値を表示する。ただし、基準値が負、数値が正の場合、エラーとなり、基準値と数値が負の場合、数値以上の最小値となる。'],[27,'ABS(数値)','指定された数値の絶対値（ある数が0からどれだけはなれているか）を表す値を表示する。'],[28,'RANDBETWEEN(最小値,最大値)','［画像の冒頭部分は判読困難］最小値以上最大値以下の乱数を返す。なお、最小値および最大値は正と負どちらも設定できる。'],[29,'FORCAST(x,既知のy,既知のx)','回帰直線上の値を返す。'],[30,'MEDIAN(数値1,数値2,・・・)','指定された範囲（数値）の中央値を求める。'],[31,'MODE(数値1,数値2,・・・)','指定された範囲で、最も頻繁に出現する値（最頻値）を求める。'],[32,'OFFSET(参照,行数,列数,[高さ],[幅])','指定した参照から行数と列数だけシフトした位置にあるセル範囲の参照を返す。セル範囲は［高さ］と［幅］で指定された範囲となる。'],[33,'COLUMN(参照)','参照で指定したセルの列番号を求める。引数を省略した場合は、関数を入力したセルの列番号を表示する。'],[34,'ROW(参照)','参照で指定したセルの行番号を求める。引数を省略した場合は、関数を入力したセルの行番号を表示する。'],[35,'SUBSTITUTE(文字列,検索文字列,置換文字列,置換対象)','文字列中の特定の文字（検索文字列）を他の文字（置換文字列）に置き換える。「置換対象」を指定すると、検索文字列中に複数の対象文字列がある場合、「置換対象」で指定した番号の文字列のみ置き換える。「置換対象」を指定しないとすべての文字列を置き換える。'],[36,'IFERROR(値,エラーの場合の値)','値に設定した式がエラーのときに、エラーの場合の値を返す。エラーではないときは、値に設定した式の値に返す。'],[37,'DSUM(データベース,フィールド,条件)','データベース内の指定された対象範囲のフィールド（列）を検索し、条件を満たすレコードの合計を求める。'],[38,'DAVERAGE(データベース,フィールド,条件)','データベース内の指定された対象範囲のフィールド（列）を検索し、条件を満たすレコードの平均を求める。'],[39,'DMAX(データベース,フィールド,条件)','データベース内の指定された対象範囲のフィールド（列）を検索し、条件を満たすレコードの最大値を求める。'],[40,'DMIN(データベース,フィールド,条件)','データベース内の指定された対象範囲のフィールド（列）を検索し、条件を満たすレコードの最小値を求める。'],[41,'DCOUNT(データベース,フィールド,条件)','データベース内の指定された対象範囲のフィールド（列）を検索し、条件を満たすセル（数値が入力されているセル）の個数を求める。'],[42,'DCOUNTA(データベース,フィールド,条件)','データベース内の指定された対象範囲のフィールド（列）を検索し、条件を満たすセル（データ（空白以外すべて）が入力されているセル）の個数を求める。'],[43,'INSERT INTO ～ VALUES','データベースの表に新しいレコード（行）を追加する。'],[44,'UPDATE ～ SET ～ WHERE ～','データベースのレコードの内容を更新する。'],[45,'DELETE FROM ～ WHERE','データベースのレコードを削除する。'],[46,'DISTINGT','重複データを取り除く。'],[47,'LIKE','文字列のデータの中から、指定した文字列を検索する。検索にはワイルドカード（＿,％）が使用できる。'],[48,'ORDER BY ～ (ASC,DESC)','データを並べ替える。ASCを省略すると昇順、DESCを指定すると降順に並べ替える。'],[49,'GROUP BY ～ (HAVING)','データをグループ化する。'],[50,'BETWEEN','ある値が特定の範囲にあてはまるかどうか調べる。'],[51,'IN(NOT IN)','値の集合を明確に定義する。'],[52,'副問合せ','ある表に対して問い合わせを行い、その結果を利用して次の問い合わせを行う。'],[53,'EXISTS句を用いた副問合せ','データが存在するか調べる。']
].map(([id,term,explanation])=>({id,question:explanation,choices:[],answer:term,explanation:`正解：${term}`}));

const app=document.querySelector('#app'); let mode, queue=[], index=0, results=[], selected=null, review=false, retryIds=[];
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function shuffle(a){return [...a].sort(()=>Math.random()-.5)}
function optionSet(p){const others=problems.filter(x=>x.id!==p.id).map(x=>x.answer);return shuffle([p.answer,...shuffle(others).slice(0,3)]);}
function modeScreen(){app.innerHTML=`<div class="card"><p class="lead">解説・意味から、対応する用語を選びます。</p><p class="notice">資料には元の問題文・選択肢・正解欄がないため、掲載された「用語」と「解説・意味」を忠実に問題と正解に使用しています。回答候補は資料内の用語のみです。</p><h2>出題モードを選択</h2><div class="mode-grid"><button class="mode" data-mode="normal"><strong>📝 通常モード</strong><span>問題番号順に出題</span></button><button class="mode" data-mode="random"><strong>🎲 ランダムモード</strong><span>問題をランダムな順番で出題</span></button></div></div>`;app.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>start(b.dataset.mode,problems));}
function start(nextMode,list,isReview=false){mode=nextMode;queue=mode==='random'&&!isReview?shuffle(list):[...list].sort((a,b)=>a.id-b.id);index=0;results=[];selected=null;review=isReview;renderQuestion();}
function renderQuestion(){const p=queue[index], pos=index+1, options=optionSet(p), label=review?'復習':(mode==='random'?'ランダムモード':'通常モード');app.innerHTML=`<div class="card"><div class="topline"><span class="badge">${label}</span><span>問題 ${pos} / ${queue.length}</span></div><div class="progress"><i style="width:${(index/queue.length)*100}%"></i></div><p class="question-number">問題番号 ${p.id}</p><div class="question">${esc(p.question)}</div><div class="choices">${options.map(v=>`<button class="choice" data-answer="${esc(v)}">${esc(v)}</button>`).join('')}</div><div id="feedback"></div><div class="actions"><button id="next" class="button" disabled>${pos===queue.length?'結果を見る':'次へ'}</button></div></div>`;app.querySelectorAll('.choice').forEach(b=>b.onclick=()=>answer(p,b.dataset.answer));app.querySelector('#next').onclick=next;}
function answer(p,value){if(selected!==null)return;selected=value;const correct=value===p.answer;results.push({id:p.id,answer:p.answer,selected:value,correct});app.querySelectorAll('.choice').forEach(b=>{b.disabled=true;if(b.dataset.answer===p.answer)b.classList.add('correct');else if(b.dataset.answer===value)b.classList.add('wrong');});const kind=correct?'good':'bad';app.querySelector('#feedback').innerHTML=`<div class="feedback ${kind}"><strong>${correct?'正解！':'不正解'}</strong>${review?`<br>あなたの回答：${esc(value)}<br>${esc(p.explanation)}`:''}</div>`;app.querySelector('#next').disabled=false;}
function next(){if(index===queue.length-1){finish();return}index++;selected=null;renderQuestion();}
function rate(correct,total){return (correct/total*100).toFixed(1).replace(/\.0$/,'')}
function finish(){const correct=results.filter(x=>x.correct).length, wrong=results.filter(x=>!x.correct);if(review){retryIds=wrong.map(x=>x.id);if(!retryIds.length){app.innerHTML=`<div class="card"><p class="success">🎉 全問正解！<br>復習完了！</p><p>${queue.length}問中${correct}問正解（復習正答率：${rate(correct,queue.length)}％）</p><div class="actions"><button class="button" id="home">モード選択に戻る</button></div></div>`;app.querySelector('#home').onclick=modeScreen;return}resultScreen(correct,wrong,true);return}retryIds=wrong.map(x=>x.id);resultScreen(correct,wrong,false);}
function resultScreen(correct,wrong,isReview){const total=queue.length;app.innerHTML=`<div class="card"><h2>${isReview?'復習結果':'テスト結果'}</h2>${!wrong.length?'<p class="success">🎉 全問正解！</p>':''}<div class="stats"><div class="stat"><b>${correct}</b>正解数</div><div class="stat"><b>${wrong.length}</b>不正解数</div><div class="stat"><b>${rate(correct,total)}％</b>${isReview?'復習':'正'}答率</div></div><p>${total}問中${correct}問正解</p>${isReview?'':`<p>出題モード：${mode==='random'?'ランダムモード':'通常モード'}</p>`}${wrong.length?`<p>間違えた問題番号：</p><ul class="wrong-list">${wrong.map(x=>`<li>問題 ${x.id}</li>`).join('')}</ul>`:''}<div class="actions">${wrong.length?'<button class="button" id="review">間違えた問題を復習する</button>':''}${!isReview?'<button class="button secondary" id="again">もう一度挑戦する</button>':''}<button class="button secondary" id="home">モード選択に戻る</button></div></div>`;if(wrong.length)app.querySelector('#review').onclick=()=>start(mode,problems.filter(p=>retryIds.includes(p.id)),true);if(!isReview)app.querySelector('#again').onclick=()=>start(mode,problems);app.querySelector('#home').onclick=modeScreen;}
modeScreen();
